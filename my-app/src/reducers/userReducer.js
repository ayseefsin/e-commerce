const initialState = {
  // Initial user state
};

const userReducer = (state = initialState, action) => {
  switch (action.type) {
    // Add action cases here
    default:
      return state;
  }
};

export default userReducer;
